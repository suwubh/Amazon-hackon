# Lessons

One lesson per entry, newest on top, one-line summary first. Read this every fresh session. Don't duplicate what CLAUDE.md/architecture.md already record.

## End-to-end audit + hero fix — pre-MT5 (June 14)
- **The hero "bug" was demo DATA, a THIRD time — and this time it was the centerpiece.** The whole live "wow" beat (SL-001 delta-grade) was showing `same_unit:false` + "a completely different product (Nike shoe) → flagged for manual review", because the seeded `current_1.jpg` was a muddy Nike against a North Star day-0 baseline. The pipeline was correct; the *photos* were a mismatch. Fix was pure data: drop in real **same-shoe** photos (clean day-0 + worn current of one physical shoe) in BOTH `backend/app/seed/images/SL-001/` and `frontend/public/items/SL-001/`, then `python scripts/capture_cache.py SL-001`. Result: `same_unit.verified:true`, honest grade D (4 real defects), no review flag — grade unchanged so spine math (+₹83/−₹129) stayed put. **Lesson: when grading looks wrong, suspect the seed photos before the prompt/code — and the demo CENTERPIECE deserves real same-unit photos, not stock placeholders.**
- **Deliverable numbers drift from the engine — reconcile PRD/script to the live API before submission.** The PRD §5 success table, a §4 inline example, and the demo script all quoted `+₹290 / −₹70` for the hero; the live VRS gives `+₹83 / −₹129` (grade D) and never produces +₹290/−₹70 at any grade. These were pre-implementation illustrative numbers that were never re-synced to the LOCKED constants. The team's own differentiator is "auditable money math" and invites judges to open the network tab — so a screen/narration mismatch is self-inflicted. **Rule: every number in PRD.md + demo-and-prfaq.md must equal a live `/route` (or `/metrics`, `/health-card`) field; recompute and reconcile whenever a constant or the hero grade changes.**
- **"Stack (LOCKED)" docs drifted from the deployed env.** CLAUDE.md + PRD §6 + the demo narration claimed "Bedrock Nova primary, Gemini failover", but the Lambda env runs `LLM_PRIMARY=gemini` (the grading-hardening pass flipped it for free-tier headroom) — the live `/grade` `source` is literally `live-gemini`, and the UI's `SourceTag` shows it on screen. For an AWS-track project this is both an optics risk and a doc-truth bug. Decision: keep Gemini-primary, make the docs honest (provider-agnostic framing, Nova = AWS-native failover, one-env-var swap) rather than flip the env. **Lesson: `deploy.ps1` ships code only; the Lambda env is the source of truth for the provider chain — keep the "LOCKED" stack line synced to what's actually set, and remember the on-screen `source`/`model` fields expose any mismatch to judges.**
- **Per-instance in-memory stores (resell/returns/cart/passport) are the recurring demo-fragility theme** — flagged again in the audit (MED 6). They work on one warm Lambda and break across a cold start / parallel instance. The durable answer is the DynamoDB table already wired for the passport; until then, keep these beats off the 3-min spine and on one pre-warmed instance.

## Landing guest identity — pre-MT5 (June 14)
- **One hardcoded `PERSONA="rahul"` was the source of the resell/ops "mixup" the user flagged.** It gated cart/orders/notifications/size-advice (storefront sample) AND resell ownership (`createListing` owner, `FlashDeals` `l.owner===persona`, `MyResells` `filter(l.owner===persona)`) AND return attribution — so every demo person was the same identity: My-resells ≡ Flash-deals, returns un-attributable. Fix split it into **`SAMPLE="rahul"`** (shared storefront sample — kept shared per the user's choice) and **`me = guest?.id || "rahul"`** (the per-browser guest id) used ONLY for the four ownership spots. The backend needed NO change — `owner`/`persona` is already a free string, so distinct ids just work.
- **Guest identity = `localStorage.sl_guest` `{id, role}`, minted on first role pick.** `genGuestId()` = `"Guest-" + Math.random().toString(36).slice(2,6).toUpperCase()` (readable + unique enough for a demo; it doubles as the display label AND the ownership match key, so one field does both). `pickRole(role)` keeps the id and updates the role; `newGuest()` mints a fresh id (lets a presenter simulate a second neighbour in one browser). Big landing buttons call `pickRole` then navigate ("login sets identity, buttons enter").
- **The two-person resell cross-tab demo now REQUIRES two identities.** Before, both tabs were "rahul" so FlashDeals hid "I'm interested" on the owner's own listing — the cross-tab interest only worked via the random `_INTEREST_POOL` on `addInterest`. With unique ids: reseller=Guest-A (one browser), buyer=Guest-B (incognito/2nd browser) → Guest-B sees Guest-A's listing as buyable → interest → Guest-A's My-resells → sell. Same browser shares localStorage → same id → back to the old collapse; use the "+ New guest identity" reset or a 2nd browser.
- **CORS still bites on the dev fallback port.** 5173 was occupied so `npm run dev` fell back to **5174**, which is NOT in the Lambda `ALLOWED_ORIGINS` → every API call "Failed to fetch" (empty shop grid). The landing + identity chip render fine (pure state, no API), so it's enough to verify this feature; for any API-touching screen, free up 5173 first (see the MT3 CORS lesson).

## Grading correctness hardening — pre-MT5 (June 14)
- **The grading "bug" was demo DATA + prompt + a missing gate, NOT the plumbing.** The code passes the uploaded current photo correctly. The real causes: (1) the SL-001 seed baseline is a clean **North Star** sneaker but the seeded `current_1.jpg` is a muddy **Nike** — two different shoes, so every grade was a delta against a stranger's product (canonical cache was rightly `same_unit:false / D`). Any user upload is compared against this fixed stock shoe, so arbitrary uploads grade garbage. (2) The prompt was **defect-biased** — "defects: *every visible difference* from day-0... empty list only if *truly indistinguishable*" + rubric "A = indistinguishable" — so lighting/angle/crop/compression differences became "defects" and dragged an identical-but-recaptured photo down to **B**. Fixed: rubric now grades *physical condition change*, and the prompt explicitly says ignore lighting/angle/distance/background/shadows/image-quality. (3) **No identity gate** — a random image still got a confident letter grade. Added a trust gate in `grade_item`: `needs_human_review` + `review_reason` when `same_unit.verified` is false / `same_unit.confidence<0.50` / `confidence<0.70`. **Verified after the fix:** upload the catalog photo as current → A/A/A (same_unit true, 0 defects); seeded Nike mismatch → D/D/D (same_unit false, flagged), all live-gemini, 3/3 consistent.
- **Gemini 2.5 Pro's FREE tier is quota-walled — not usable as a live primary.** A vision probe returned `429 RESOURCE_EXHAUSTED` on `generate_content_free_tier_input_tokens`, twice, minutes apart (a daily cap, not a blip). On stage it would 429 and silently fall to Nova. **Gemini 2.5 Flash works** (verified — correctly read "North Star white shoes…") and has the free headroom. The `AQ.Ab8…`-format key is valid (a 429 means it authed). So the chain is **Gemini 2.5 Flash (primary) → Nova 2 Lite (failover) → cache**. (If you ever need zero external quota, Nova **Pro** on Bedrock is the one-env-var swap funded by the $200 credits — `LLM_PRIMARY=bedrock` + `BEDROCK_MODEL_ID=<nova-pro id>`.)
- **Gemini 2.5 Flash is a THINKING model: reasoning tokens eat `max_output_tokens` and truncate the JSON mid-object.** The raw output for a harder (defects-present) case was only 68 chars — cut off right after the `same_unit` block — so JSON parse failed *deterministically* (same char position every run) and it fell back to cache. `response_mime_type="application/json"` alone did NOT fix it (the issue is length, not format). The fix is **`thinking_config=ThinkingConfig(thinking_budget=0)`** for the vision calls (structured extraction needs no extended thinking; also faster/cheaper). Both set in `_ask_gemini_vision`. Also added an 18s `HttpOptions(timeout=…)` so a hung Gemini primary fails over to Nova inside the Lambda 30s budget (Gemini had no timeout before; only Bedrock did).
- **`deploy.ps1` ships CODE ONLY — Lambda env vars are set separately, and `update-function-configuration --environment` REPLACES the whole map.** To flip `LLM_PRIMARY`/`LLM_FALLBACK` + add `GEMINI_MODEL` without wiping `GEMINI_API_KEY`/`ALLOWED_ORIGINS`/`DYNAMODB_TABLE_NAME`: `get-function-configuration` → merge → re-apply the full map via `--environment file://…json` (commas in `ALLOWED_ORIGINS` break the inline shorthand). Wait `function-updated`, then `deploy.ps1` for the code. Verified live on the Function URL afterward (never trust "Done. Deployed"): `/grade` → `live-gemini / gemini-2.5-flash`, gate fields present; `/route` SL-001 still `local_p2p +₹83`.
- **The re-grade ripples beyond SL-001 — re-capture caches when model/prompt change.** `scripts/capture_cache.py SL-001 SL-002` re-baked both grade caches with Flash. SL-001 stays **D** (Nike mismatch) so the hero **+₹83** math is unchanged, but **SL-002 (monitor) now grades A** (its seeded photos genuinely match) → its radar/resell recovery shifts up. Cache stores *perception facts only*; the trust gate (`needs_human_review`/`review_reason`) is computed at serve time, so cached and live behave identically. When real same-shoe SL-001 photos land, re-capture again.

## MT10.1 — resell sell-to-buyer + real-time inspectable returns + one resell path (June 14)
- **The resell marketplace was list-only — no way to accept a buyer.** `MyResells` was a read-only interest feed; `resell.py` listings had no sale concept. Added `status`/`sold_to`/`net_earned` to the listing, `sell_to_interest(lid, interest_id)` (flips listing→sold, accepted buyer, others→`passed`, net = offer − delivery_cut) and `decline_interest` (marks one interest `declined`, listing stays active), exposed as `POST /resell/listings/{id}/sell|/decline`. `MyResells` now renders per-buyer Sell/Decline with the take-home, a SOLD summary, and pauses its 3s poll during an action so the optimistic flip isn't clobbered. Verified live: declined Kabir → sold to Asha → SOLD + ₹1,683 credited; FlashDeals shows the seller's own listing as "Your listing" and sold ones as "Sold nearby".
- **Buyer returns weren't real-time because `openOps()` overwrote them with a cross-instance refetch.** The buyer `POST /returns` and the Ops `GET /returns` can hit different warm Lambdas (per-instance store), so the new row vanished on the Ops desk. Fix: keep a session-local `localReturns` in App (the optimistic entry, built from the order + catalog item), MERGE it ahead of the server list (dedup by `order_id`, local wins), and never let the fetch clobber it. The row now shows instantly and survives the instance swap. The POST is still fired best-effort so a same-instance refetch also has it.
- **Returned items were display-only — you couldn't grade them.** Dynamic return rows rendered as non-clickable `QueuedReturn`. Added `item_id` to the return entry (`returns.py`) so a row that resolves to a catalog item becomes a clickable `GradeableReturn` ("Inspect & grade") that opens the SAME spine as the hero (ItemIntro→Grade→Route→HealthCard) via `openReturnForGrading` (forces `lane="spine"`, bypassing the LANE map so SL-003 doesn't open Diagnose). Photoless seeded rows stay `QueuedReturn`. **Needed a cached grade for the kurta:** added `cached/SL-003.grade.json` (apparel, grade B, collar defect, "colour not as shown" justification) so the returned-kurta inspection works in BOTH live and CACHED modes (SL-003 has no seed images → live grade of no-upload falls through to this cache; before, it 502'd).
- **The user overrode a prior "keep both resell paths separate" decision.** MT10 had kept the notification "Resell now" on the old `/radar`→`/price-curve` flow while order-history used the new confirm→photo→price→list flow. The user wanted ONE path. `openNotif` now calls `startResell` (looking up the matching order in `buyerOrders` for the real paid/date), and `resellOrder` was deleted (orphaned). The Idle Asset Radar beat is still reachable from the Ops inbox SL-002 lane, so that story isn't lost. Lesson: a documented design choice is provisional — when the user says unify, unify.

## MT10 (June 13)
- **DEPLOY BLOCKER RETURNED then FIXED for good: route ECR through the `ecr-login` cred helper.** `deploy.ps1`'s `docker login` to ECR returns **400 Bad Request** (Docker `credsStore: desktop` mangles the login), and this session the previously-cached ECR token had **expired**, so `docker push` failed with **403 Forbidden** on a blob — and the script still ran `update-function-code`, so Lambda silently kept the OLD image (confirmed: deployed `/returns` 404'd after a "Done. Deployed"). **ALWAYS confirm a deploy by curling a NEW endpoint on the Function URL, never trust "Done. Deployed".** Permanent fix applied to `~/.docker/config.json`: added `"credHelpers": {"656751413989.dkr.ecr.ca-central-1.amazonaws.com": "ecr-login"}` (the helper `docker-credential-ecr-login.exe` ships with Docker Desktop and pulls a token from the AWS creds chain automatically — no `docker login` needed). After that, `docker push` succeeded (`latest: digest: sha256…`) and `aws lambda update-function-code` + `wait` made the new endpoints live. A backup is at `~/.docker/config.json.mt10bak`. `deploy.ps1` itself is unchanged (its login line still prints 400, now harmless because the helper authenticates the push).
- **Windows uvicorn `--reload` is flaky AND a killed background server can orphan a socket on its port.** `--reload` didn't pick up edits (watchfiles detected the change but the worker served stale code); use plain `uvicorn` and restart manually after each backend edit. Worse, after killing a background uvicorn, port 8080 stayed bound by a PID that `Get-Process`/`taskkill` couldn't find (the process lived in the Bash-tool sandbox, invisible to host PowerShell) — `/returns` 404'd because the zombie served old code. Fix that unblocked me: **run local verification on a fresh port (8090)**. Servers started via the PowerShell tool (vs Bash background) ARE killable with `taskkill /F /PID` from PowerShell.
- **Iron rule held for a whole marketplace via thin seed+route.** Returns desk, resell quote/listings/interest all trace to endpoints. Resell economics reuse `pricing.py` + `seed.buyers_for_asin(asin, range_km)` with REAL seeded buyer geography — the monitor (B0MONITOR1) has buyers at 1.9/3.5/4.4/8.4 km, so range tiers 3/7/15 km give 1/3/4 reachable buyers and **net peaks mid-range** (935@3km → 1009@7km → 961@15km) because `delivery_cut = 25 + 6·km` grows faster than the demand-multiplier price lift past 7km. That trade-off is the seller-facing insight, and it's derived, not hardcoded.
- **Resell is order-history entry; the idle-radar resell beat is separate.** Orders→Resell now runs the NEW flow (confirm→photo→AI price→price/range→list→board→interest). The SL-002 notification "Resell now" nudge KEEPS the old `/radar`→`/price-curve` flow (`resellOrder`), so both stories coexist — don't collapse them.
- **Return window + buyer→ops link both need recent data.** Rahul's seed orders were all 2024 (window closed). Added 2 orders dated within 10 days of the demo date 2026-06-13 so the Return button is demoable; a buyer Return `POST /returns` (per-instance store) makes the row appear on the Ops desk. Extra/dynamic return rows are **display-only** ("queued for grading") — only SL-001 is live-gradeable, which keeps the desk bulletproof on the photoless filler items.
- **Cross-tab interest is real but per-instance.** Listings/interests live in an in-memory store; cross-tab works rock-solid on local uvicorn (one process) and on a single warm Lambda. A cold start or a second parallel instance loses it — same caveat as the cart/passport. For the stage, keep both tabs on one warm instance (just don't idle 10+ min between).

## MT8 + landing-restore (June 13)
- **The user wanted the ORIGINAL dark-stage landing back, not the MT9 web-grid Home.** MT9 had re-laid the landing as a paper-background persona grid inside `WebShell` — the user found it "really bad" and wanted the pre-MT9 `PhoneFrame` brand-rail look (dark `stage-bg`/`stage-grain`, "Every product finds its next best owner" + description + the live-engine line, original colors), just with the **phone replaced by three buttons** (Returns desk · Buyer · Seller, each label + one-line role). Fix: rewrote `Home.jsx` as a full-screen `stage-bg` flex layout (brand rail + a 3-button column where the phone sat) and made `App.jsx` render `Home` **outside** `WebShell` (`if (screen==="home") return <Home/>`), so the landing has **no top bar**. The dark-stage CSS classes survived MT9's de-phone (still in index.css), so this was a rewrite of one screen + a one-branch App change, not new CSS.
- **Slimming shared chrome: the landing and inner pages want DIFFERENT shells.** The user wanted the inner top bar to show ONLY leaf + "Second Life" (drop search/bell/cart/"a layer inside Amazon"). Rather than prop-toggle `WebShell` per screen, the clean split is: landing = its own full-bleed component (no shell), inner pages = the slim `WebShell`. Removed `cartCount`/`onCart` from `WebShell` — the buyer cart is still reachable via BuyerStore's in-page Cart tab, so the global cart button was redundant.
- **MT8's derived cascade is only as deep as the LOCKED VRS constants allow — and that's correct, not a bug.** `donate` net = 0.15·MRP − 30 dominates `liquidate` (0.12·MRP − 20) for every MRP > ~333, and `local_p2p` always beats `warehouse_relist` (higher kept sale, far lower cost). So for SL-001 the cascade is just `local_p2p → donate` (2 tiers); no item produces the doc's illustrative 4-tier local→warehouse→liquidate→donate. Do NOT "fix" this by tuning constants (they're LOCKED, architecture §4, and the spine reconciliation depends on them). Higher-value/repairable items (SL-006 grade C → refurbish→donate) go deeper. The honest 2-tier shoe waterfall still answers "what happens to items that don't sell locally."
- **Refactor `route_item` → `build_paths()` + `grade_for()` to share the path math with cascade, WITHOUT changing spine output.** The cascade re-runs the same argmax at decayed prices, so the path builder had to become a pure, side-effect-free function (no passport writes, takes `resale`/`refurb_resale` as params). Verified the spine is byte-identical after the refactor (local_p2p +₹83, every eligible path's recovery == sum(breakdown)). The mrp-anchored floors (donate/liquidate/rto) DON'T decay (credit/bulk are on MRP fair value, not time-sensitive resale) — that's exactly why the cascade falls THROUGH the decaying resale channels down to the constant donate floor.
- **`/cascade` requires a prior GRADED event (409 otherwise) — so it only renders on the spine RouteScreen in the demo.** The RTO lane has no GRADED (sealed units skip grading), and the radar/QUEUED lanes never reach RouteScreen. So App fetches the cascade **non-blocking** after `routeSafe` on the spine lane only and passes `cascade={null}` for RTO. `cascadeSafe` mirrors `routeSafe` (re-grade once on a cold-start 409).
- **The ECR-login 400 is STILL cosmetic (confirmed again this session).** `deploy.ps1` printed the 400 on the login line, then `docker push` succeeded (`latest: digest: sha256…`) and `Done. Deployed`. Check for the digest + "Done", not the login line.

## MT9 (June 13)
- **De-phoning was a one-line CSS lever + a shell swap, not a per-screen rewrite.** Every screen used `.screen-scroll` (height:100%/inner-scroll inside the phone). Redefining `.screen-scroll` in `index.css` to a centered reading column (`max-width:44rem; margin-inline:auto`) and deleting `.phone`/`.phone-screen` de-phoned ALL flow/spine screens at once with zero per-screen edits. Wide screens (Home/BuyerStore/SellerDashboard/ItemIntro two-pane) opt into `.screen-page` (full width) + their own inner max-width. `WebShell` (sticky navy brand bar + `max-w-6xl` column) replaced `PhoneFrame`; `TopBar` lost the iOS status row and became a light page header.
- **`TopBar` going light broke only ONE consumer:** Inbox's `StageToggle` was styled for the dark phone bar (`bg-white/10 text-white/55` → invisible on paper). Fixed it to a light pill. `SLBadge` (mint) and `SourceTag` (mint/grey) already read on light. When restyling a shared chrome component, grep its `right=` slot consumers.
- **Two stacked sticky bars overlap.** `WebShell`'s brand bar is `sticky top-0`; making the per-screen `TopBar` also sticky-top-0 would overlap it. Kept `TopBar` non-sticky (normal flow) — the persistent brand bar covers "where am I", the sticky-bottom `FooterAction` covers the CTA. Also moved `RadarToast` + the error note from `absolute` (phone-relative) to `fixed inset-x-0 bottom-0` + `mx-auto max-w-md` so they pin to the viewport, centered, on web.
- **Hybrid photo upload, zero deps:** `lib/image.js` uses a canvas (`drawImage` → `toDataURL("image/jpeg", 0.72)`, long edge ≤1024px) to downscale a phone photo to ~300 KB base64 before POSTing. Backend `/grade` `current_images` (≤3, ≤1.5 MB decoded each) decodes+validates BEFORE any LLM call (422 fast-fail), uses them as the CURRENT set against the seeded catalog+day-0 baseline; `_grade_live(item, current_override)`. Verified live: the `/grade` body carried `current_images` and Bedrock graded the uploaded photo (`graded_uploaded_photos:true`). A judge can open the network tab and see it.
- **Per-instance cart bit me on checkout, visibly.** The cart is an in-memory overlay (like the passport), so a `/checkout` confirm landing on a *different* warm Lambda recomputed the total off that instance's (seeded) cart → success showed ₹1,399 when the user approved ₹1,899. Fix: the frontend pins the approved `order_id` + `amount` from the pending response and the confirm only flips `status` (`setCheckout(prev => ({...co, order_id: prev.order_id, amount: prev.amount}))`). Lesson: for per-instance state, never re-read a money figure the user already saw on a second call — carry it forward.
- **chrome-devtools `upload_file` drives a hidden file input via the trigger button's uid** (point it at the "Upload photos" button, not the `<input>`), and the canvas-downscaled preview shows as a `data:image/jpeg;base64,...` URL in the a11y snapshot — handy to confirm the downscale ran.
- **DiagnoseScreen's projected-drop is −0% until "Apply the fix"** (`useCountUp(applied ? pct : 0)`), then it counts to −40%. Looks like a stuck value pre-apply but it's by design — don't "fix" it.

## MT4 (June 13)
- **chrome-devtools MCP won't attach if a stale Chrome from a prior session is still holding its profile** (`...\.cache\chrome-devtools-mcp\chrome-profile`) — every `navigate_page`/`new_page` errors "The browser is already running … Use --isolated". Fix: kill ONLY the MCP-owned tree, not the user's Chrome — `Get-CimInstance Win32_Process -Filter "Name='chrome.exe'" | Where CommandLine -like '*chrome-devtools-mcp*' | Stop-Process -Force`, then navigate again. (Don't blanket-kill chrome.exe.)
- **`/metrics` is cumulative over the warm Lambda instance, not per-session.** It's `BASELINE (5 items / ₹3,120 / 80%) + every GRADED/ROUTED event since the last cold start`. So routing items in testing (curl or the UI) bumps it permanently for that instance — prod read ₹3,203/83% after my test routes. Good (proves numbers are live, not hardcoded) but the inbox/metrics "this session" label drifts up across demo runs. For a clean stage number, hit a cold instance or add a reset (MT5).
- **Two new cold-start-safe wrappers, same pattern as routeSafe/healthCardSafe:** `priceCurveSafe` (price-curve 409s without a GRADED event → grade *cached* then retry; the grade is a hidden idle-asset valuation, letter is invariant, so force cached) and `routeRto` (route 409s without a SEAL_CHECKED event → re-run seal-check then route). The 1 benign 409 you'll see in the prod console on the radar lane IS priceCurveSafe's first probe — the slider still renders, so it's the retry working, not a bug.
- **Reused RouteScreen for the RTO lane via an optional `nextLabel`/`nextHint` prop** instead of a second route screen — RTO is seal→route→list (no Health Card; it's the "15-second" beat). A sealed RTO routes as grade A and `rto_relist` wins **+₹2,464** (breakdown reconciles: 2519−15−40), beating warehouse +₹1,810.
- **The sticky FooterAction's gradient is transparent at the top (where the hint sits), so on SHORT screens the hint text overlaps a real data row.** Fix that scales: added `backdrop-blur-sm` to the footer (frosted reveal — content under the CTA blurs, looks intentional on every screen) AND dropped the now-redundant hint on the shortest screen (SealLane). Long scrollable screens were always fine — content fading under the CTA is the intended look (matches the spine screens).
- **`/metrics`, `/radar`, `/price-curve`, `/seal-check`, `/diagnose-listing` all already existed from MT2** — MT4 was pure frontend, so NO backend redeploy. Only `git push` → Vercel auto-deploy. Confirm contracts with a quick curl against the Function URL before wiring (every number on screen must trace to a field).

## MT3 (June 13)
- **CORS is origin-exact: use `http://localhost:5173`, NEVER `127.0.0.1:5173`.** The Lambda `ALLOWED_ORIGINS` env = `http://localhost:5173,https://amazon-hackon.vercel.app`. A browser at `127.0.0.1:5173` is a *different* origin → every fetch dies with "Failed to fetch" (CORS, no Allow-Origin header) even though curl works fine. Drove me in a circle for one cycle. To add an origin, edit the Lambda env var — no redeploy/rebuild needed.
- **The Vercel prod URL is https://amazon-hackon.vercel.app and it auto-deploys on `git push origin main`** (was live <1 min after push; detect the new build by the `<title>` flipping from `frontend` → `Amazon Second Life`). Lambda env already had `ALLOWED_ORIGINS` + `DYNAMODB_TABLE_NAME=SecondLifePassport` set (by the human), so MT3 needed NO backend change/redeploy.
- **Baked the deployed Function URL as the api.js fallback** (`VITE_API_URL || DEPLOYED`) so the Vercel build works even if the dashboard env var is unset. `.env.local` also points at the deployed backend so `npm run dev` needs no local uvicorn. The URL is already public in api-spec.md — fine to bake.
- **Missing item photos don't break the UI:** `frontend/src/components/Thumb.jsx` has an `onError` → category-tile fallback. SL-003..008 have no images → 6 benign 404s in console, handled. Only SL-001/SL-002 ship real photos; copy them to BOTH `backend/app/seed/images/` and `frontend/public/items/SL-00X/` (frontend thumbs are static, not a backend route).
- **Frontend defends the per-instance-passport 409:** `api.routeSafe`/`api.healthCardSafe` catch a 409 (cold Lambda lost the prior GRADED/ROUTED) and re-run the prerequisites once, so the spine never dead-ends on a warm→cold swap. The natural screen order (scan→route→card) keeps one warm instance anyway.
- **DynamoDB write-through works** — after a live grade+route, `aws dynamodb query --table-name SecondLifePassport ... :i={S:SL-001}` returns GRADED + ROUTED rows (db-setup.md §4 satisfied). NOTE: `/health-card`'s documented `LISTED` event did NOT show up in DynamoDB during testing — non-blocking, but check if a listed-count ever matters.
- **Playwright MCP saves screenshots into its own sandbox** (unreachable from the Bash tool's CWD) and dumps `.playwright-mcp/` + `*.png` into the repo root — clean those before committing. For screenshots I can actually SEE inline, use the **chrome-devtools MCP** `take_screenshot` (returns the image to me) instead of playwright's (path-only).
- **Tailwind v4 `@theme` tokens worked first try** for the design system: `--color-az-navy`, `--color-sl-green`, `--font-display` in `index.css` → `bg-az-navy`, `text-sl-green`, `font-display` utilities. Fonts via Google `<link>` in index.html (Bricolage Grotesque + Plus Jakarta Sans — deliberately NOT Inter). Build is ~240KB JS / 37KB CSS, no extra deps.

## MT2 (June 13)
- **The IAM deploy blocker is RESOLVED.** `deploy.ps1` now pushes to ECR and runs `aws lambda update-function-code` successfully; the new image is live on the Function URL. Quirk: the `aws ecr get-login-password | docker login` step still prints `400 Bad Request`, but `docker push` succeeds anyway because Docker reuses cached ECR creds from a prior good login — so a 400 on the login line is NOT a deploy failure; check for `latest: digest: sha256...` and `Done. Deployed` at the end instead.
- **deploy.ps1 must run from `backend/`** (its build context is `.`). The PowerShell tool's working dir was already `backend`, so a leading `Set-Location backend` errored with `backend\backend not found` — harmless, the script still ran from the right place. If you script it, guard the cd or just run `./deploy.ps1` from backend.
- **Background shells (`run_in_background`) start in a fresh CWD, not the persisted one** — use absolute paths (`cd "C:/.../backend"`) when launching uvicorn/scripts in the background, or you get `cd: backend: No such file or directory`.
- **Windows console is cp1252 — printing the ₹ char (U+20B9) crashes with UnicodeEncodeError.** Set `PYTHONIOENCODING=utf-8` for any python that prints rupee strings (e.g. radar ping_message), or extract numeric fields instead of printing the whole JSON.
- **VRS integrity rule that MT3 depends on:** each path's `recovery` is the exact sum of its `breakdown` values (sale/credit positive, costs negative). The frontend renders the breakdown and the total must reconcile on screen. If you tune a constant, keep this invariant.
- **The api-spec /route example numbers were illustrative and didn't sum** (and assumed grade B). MT2 made them real: the example now reflects the live grade-D output for SL-001 and every figure reconciles. Recovery scales with grade — don't expect the +₹290 "hero" number unless the shoe is graded B (current placeholder cache grades it D → local +₹83).
- **Sealed RTO items skip grading** (architecture §3.2): `/route` normally 409s without a prior grade, but a verified-sealed RTO item routes as factory-new (grade A) with no grade required. So the RTO demo beat is seal-check → route (no scan).
- **In-memory passport is per-Lambda-instance.** `/route` and `/health-card` need the prior `/grade`/`/route` events to be in the SAME warm instance. Sequential demo curls reuse one instance so it works; a cold start (or parallel calls hitting different instances) loses the chain → 409. For a bulletproof stage, set `DYNAMODB_TABLE_NAME` (MT5) or always run the spine in order on one session.

## MT1 (June 13)
- **Deploy is blocked by IAM, not code.** The configured AWS CLI user `hackon-app` (account 656751413989) can invoke Bedrock (local grading works) but is **denied `ecr:GetAuthorizationToken`/ECR push and `lambda:UpdateFunctionCode`** → `deploy.ps1` builds fine, then fails at ECR login (400) and Lambda update (AccessDeniedException). A failed deploy changes nothing on the live function, so the demo path stays up — but **MT2 cannot ship to Lambda until `hackon-app` gets ECR-push + Lambda-deploy permissions, or deploy.ps1 is run with admin creds.** HUMAN TASK before MT2.
- **Nova-2 multimodal converse: image blocks go in `content` as `{"image": {"format": "jpeg", "source": {"bytes": <raw bytes>}}}`** (raw bytes, not base64), text block last. Worked first try in ca-central-1.
- **Grading consistency is solid at temperature=0.2**: hero shoe graded D/D/D across 3 live runs. The same-unit check is genuinely discriminative — placeholder current-photo (different shoe) correctly returned `same_unit.verified=false`; swap in real day-0-vs-now photos of the *same* unit before judging same-unit accuracy.
- **Git on Windows rewrites LF→CRLF** on these files (harmless warning); seed `.jpg`/`.json` commit fine and bake into the container via `COPY app` in the Dockerfile.

## Pre-build gotchas (carried from boilerplate phase — verified the hard way)
- **Bedrock throttles new accounts in us-east-1** → everything runs in ca-central-1.
- **Nova 2 Lite needs the inference-profile ID** `us.amazon.nova-2-lite-v1:0` (fallback `global.amazon.nova-2-lite-v1:0`), not a bare model ID.
- **Nova 2 is a reasoning model** → responses may contain a `reasoningContent` block before the text; extract the block that has `text`, never `content[0]`. Same applies to multimodal/JSON outputs.
- **Lambda container builds need `--provenance=false`** (handled in deploy.ps1); base image already has the RIC; x86_64, 512 MB, 30s timeout.
- **`load_dotenv()` must run before boto3 use locally**, else `NoCredentialsError`.
- **CORS lives in the FastAPI app only**; Function URL CORS stays disabled or it answers preflights itself.
- **Bedrock calls must be time-bounded** (connect 5s / read 15s, 2 retries) so a hung primary fails over to Gemini before the Lambda 30s timeout. Budget: with images, leave headroom — one live multimodal call + one fallback must fit in 30s.
- **Failover needs both creds present** (Bedrock via role/keys + `GEMINI_API_KEY`).
- **Don't use the full /gsd:* ceremony as the build spine** — plain task-by-task per docs/tasks.md; /gsd:debug, /code-review, /verify as point tools.
